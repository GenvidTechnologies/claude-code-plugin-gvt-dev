# Wiki Log

Append-only record of every `ingest` run: what changed, why, and which `raw/`
source drove it. Never edit or remove a prior entry — if a log entry itself
needs correcting, append a new entry that says so. See `docs/wiki-schema.md`
for the full maintenance schema.

| Date | Change | Why | Source |
| --- | --- | --- | --- |
| 2026-07-21 | Created `llm-wiki-pattern-in-gvt-dev.md` and `audit-conventions-as-proto-lint.md`; added both to `index.md` | Dogfooding #143 — standing up the wiki practice in the plugin repo itself, ingesting the two captured `raw/` sources into the first real wiki pages | #143 |
